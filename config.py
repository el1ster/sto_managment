from datetime import date

ENTERPRISE_START_DATE = date(2022, 1, 1)
# config.py
# Файл конфігурації проекту "СТО Manager"
# Всі параметри обов'язково супроводжуються коментарями (що означає і для чого використовується)

# === Параметри підключення до бази даних ===

DB_NAME = "auto_service_station_final"  # Назва основної бази даних
DB_USER = "root"  # Користувач бази даних
DB_PASSWORD = "Qwant_8772_5033"  # Пароль до БД (у реальному проєкті тримати у секретах!)
DB_HOST = "localhost"  # Хост, де розміщена БД (зазвичай localhost)
DB_PORT = 8772  # Порт, на якому працює MySQL (наприклад, 3306 або 8772)

# === Основні параметри програми ===

APP_NAME = "СТО Manager"  # Назва додатку (відображається у заголовках вікон тощо)
VERSION = "0.1.0"  # Поточна версія ПЗ
DEBUG = True  # Флаг для режиму розробки (True — виводити додаткові повідомлення, False — продакшн)
LANGUAGE = "uk"  # Мова інтерфейсу за замовчуванням (наприклад, "uk" або "en")
LOG_LEVEL = "INFO"  # Рівень логування ("DEBUG", "INFO", "WARNING", "ERROR")

# === Шляхи до ресурсів (іконок, логів, експорту) ===

RESOURCE_PATH = "./resources/"  # Каталог зі статичними ресурсами (іконки, зображення)
EXPORT_PATH = "./exports/"  # Каталог для збереження експортованих файлів (PDF, Excel тощо)
LOG_PATH = "./logs/"  # Каталог для зберігання лог-файлів

# === Налаштування email (на майбутнє, для розсилки повідомлень/логів) ===

EMAIL_ENABLED = False  # Чи ввімкнена email-розсилка
EMAIL_HOST = "smtp.example.com"  # SMTP-сервер для надсилання email
EMAIL_PORT = 587  # Порт для SMTP
EMAIL_USER = ""  # Користувач SMTP
EMAIL_PASSWORD = ""  # Пароль SMTP
NOTIFICATION_EMAIL = ""  # Email для системних повідомлень/сповіщень

# === GUI (інтерфейс користувача) ===

DEFAULT_WINDOW_SIZE = (1600, 800)  # Розмір головного вікна додатку (ширина, висота)
THEME = "dark"  # Тема оформлення інтерфейсу ("dark", "light" тощо)
DEFAULT_FONT = "Segoe UI"  # Шрифт за замовчуванням для всього додатку

# === Авторизація, безпека ===

ADMIN_DEFAULT_USERNAME = "superadmin"  # Ім'я користувача для першого адміністратора (початкова ініціалізація)
ADMIN_DEFAULT_PASSWORD = "superadmin123"  # Пароль для першого адміністратора (небезпечно для продакшну, змінити після ініціалізації!)
MAX_LOGIN_ATTEMPTS = 5  # Максимальна кількість невдалих спроб входу до блокування
SESSION_TIMEOUT = 1800  # Тривалість сесії користувача у секундах (1800 = 30 хв)

# === Резервне копіювання ===

BACKUP_PATH = "./backups/"  # Каталог для резервних копій БД
AUTO_BACKUP_ENABLED = True  # Чи увімкнено автоматичне резервне копіювання

# === Додаткові налаштування (можна додавати нові, завжди з коментарем) ===
# ...
# config.py

APP_NAME = "СТО Manager"
DEFAULT_WINDOW_SIZE = (1024, 768)

# Список фіксованих спеціалізацій
SPECIALIZATIONS = [
    "mechanic",  # Механік
    "electrician",  # Електрик
    "diagnostic",  # Діагност
    "bodywork",  # Кузовник
    "painter",  # Маляр
    "tire service"  # Шиномонтажник
]

# Статуси задач, які використовуються в усьому проєкті
TASK_STATUSES = [
    "new",  # Нова
    "in progress",  # У процесі
    "completed"  # Виконана
]
